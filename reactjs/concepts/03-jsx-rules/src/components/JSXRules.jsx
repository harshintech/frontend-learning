

const JSXRules = () => {
  return (
    <div>
      <h1>JSX Rules</h1>
      <p>
        JSX must return a single parent element<br></br>
        JSX elements must be properly closed.<br></br>
        JSX attributes are written using camelCase(e.g., className instead of class) 
      </p>
    </div>
  )
}

export default JSXRules
